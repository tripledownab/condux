{
  "name": "@condux/core",
  "version": "0.1.0",
  "description": "Condux SDK core: the isomorphic engine (Sentry store wire shape + resilient transport) shared by the Node, browser, edge and Next.js SDKs.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "prepare": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  },
  "license": "Apache-2.0",
  "homepage": "https://github.com/tripledownab/condux/tree/main/sdks/core#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tripledownab/condux.git",
    "directory": "sdks/core"
  },
  "publishConfig": {
    "access": "public"
  }
}
