# Control-plane API base URL (baked into the client bundle at build time).
NEXT_PUBLIC_CONDUX_API_URL=http://localhost:8080
# Ingest relay base URL, used to reconstruct a key's DSN on the settings page (matches the relay host).
NEXT_PUBLIC_CONDUX_INGEST_URL=http://localhost:9010
